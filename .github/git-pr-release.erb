Release <%= Time.now.strftime('%Y-%m-%d %H:%M') %>
<% feature_prs = pull_requests.select { |pr| pr.labels.include?('feature') } %>
<% if feature_prs.any? %>
## 🚀 Features
<% feature_prs.each do |pr| -%>
<%= pr.to_checklist_item %>
<% end -%>
<% end %>
<% bug_prs = pull_requests.select { |pr| pr.labels.include?('bug') } %>
<% if bug_prs.any? %>
## 🐛 Bug Fixes
<% bug_prs.each do |pr| -%>
<%= pr.to_checklist_item %>
<% end -%>
<% end %>
<% maintenance_prs = pull_requests.reject { |pr| pr.labels.include?('feature') || pr.labels.include?('bug') } %>
<% if maintenance_prs.any? %>
## 🧰 Maintenance
<% maintenance_prs.each do |pr| -%>
<%= pr.to_checklist_item %>
<% end -%>
<% end %>
---
- 本PRには `release` ラベルが自動付与されます。
  - メジャーバージョンアップしたい場合は `major` ラベルを追加してください。
  - マイナーバージョンアップしたい場合は `minor` ラベルを追加してください。
  - `major`, `minor` を設定しない場合はパッチバージョンアップになります。
